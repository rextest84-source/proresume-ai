[build]
builder = "DOCKERFILE"
dockerfilePath = "services/api/Dockerfile"
watchPatterns = ["backend/**", "services/api/**"]

[deploy]
startCommand = "node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
